spring:
  application:
    name: client
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8762         #本地配置中心的路径
      fail-fast: true                    #设置客户端优先判断ConfigServer获取是否正常
